apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/jenkins-x-apps/jx-app-cheese:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/jenkins-x-apps/jx-app-cheese:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: golang-http
        chartPath: charts/golang-http
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/jenkins-x-apps/jx-app-cheese"
          image.tag: "{{.DIGEST_HEX}}"
